import { error } from "console";

if (!Response.ok) {
    throw new Error('Failed')
}


const data : CampionRotation = await Response.json();
const champion= awiat fetchChampion();
data.freeChampion
)
return 
} catch(error)
return NextResponse.json(error)
